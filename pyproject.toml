[project]
name = "auto-repair-shop"
version = "0.1.0"
description = "Auto repair shop operations, inventory, and reporting tools."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.black]
line-length = 100
target-version = ["py311"]

